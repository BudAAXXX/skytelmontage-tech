<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Error 404 – SkyTel Montage</title>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700&display=swap" rel="stylesheet" />
  <style>
    body {
      margin: 0;
      background: #000;
      color: #0f0;
      font-family: 'Orbitron', sans-serif;
      overflow: hidden;
      text-shadow: 0 0 5px #0f0;
    }

    header {
      padding: 3rem 1rem 1rem;
      text-align: center;
      z-index: 10;
      position: relative;
    }

    h1 {
      font-size: 4rem;
      color: #00ff88;
      margin: 0;
      text-shadow: 0 0 20px #00ff88;
    }

    p.subtitle {
      font-size: 1.5rem;
      color: #8affc1;
      margin: 1rem 0 2rem;
    }

    .notice {
      margin: 2rem auto;
      padding: 1rem 2rem;
      background: rgba(0, 255, 136, 0.1);
      border-left: 5px solid #00ff88;
      color: #00ff88;
      max-width: 600px;
      text-align: center;
    }

    .slideshow {
      width: 400px;
      height: 400px;
      position: absolute;
      animation: floatAround 20s infinite alternate ease-in-out;
      z-index: 5;
    }

    .cube {
      width: 100%;
      height: 100%;
      position: relative;
      transform-style: preserve-3d;
      animation: randomRotate 10s infinite linear;
    }

    .cube div {
      position: absolute;
      width: 100%;
      height: 100%;
      background-size: cover;
      background-position: center;
      backface-visibility: hidden;
      border-radius: 16px;
    }

    /* FUNKČNÍ IMGUR OBRÁZKY */
    .front  { transform: rotateY(0deg) translateZ(200px); background-image: url('https://i.imgur.com/Uo2PP0C.png'); }
    .back   { transform: rotateY(180deg) translateZ(200px); background-image: url('https://i.imgur.com/5H9iorK.png'); }
    .right  { transform: rotateY(90deg) translateZ(200px); background-image: url('https://i.imgur.com/S1oPJm0.png'); }
    .left   { transform: rotateY(270deg) translateZ(200px); background-image: url('https://i.imgur.com/8rqsqgB.png'); }
    .top    { transform: rotateX(90deg) translateZ(200px); background-image: url('https://i.imgur.com/rrBPPKb.png'); }
    .bottom { transform: rotateX(270deg) translateZ(200px); background-image: url('https://i.imgur.com/Uo2PP0C.png'); }

    @keyframes randomRotate {
      0% { transform: rotateX(0deg) rotateY(0deg); }
      50% { transform: rotateX(180deg) rotateY(180deg); }
      100% { transform: rotateX(360deg) rotateY(360deg); }
    }

    @keyframes floatAround {
      0%   { top: 10vh; left: 10vw; }
      25%  { top: 30vh; left: 70vw; }
      50%  { top: 60vh; left: 20vw; }
      75%  { top: 20vh; left: 80vw; }
      100% { top: 40vh; left: 40vw; }
    }

    footer {
      position: absolute;
      bottom: 1rem;
      width: 100%;
      text-align: center;
      font-size: 0.9rem;
      color: #08ffaf;
      z-index: 10;
    }

    canvas#matrix {
      position: fixed;
      top: 0;
      left: 0;
      z-index: 0;
    }
  </style>
</head>
<body>
  <canvas id="matrix"></canvas>

  <header>
    <h1>Error 404</h1>
    <p class="subtitle">Stránka neexistuje… nebo právě visí v Matrixu.</p>
    <div class="notice">
      💾 Požadovaná realita nebyla nalezena.<br>
      <strong>Možné důvody:</strong><br>
      – Web je ve výstavbě<br>
      – Jsem momentálně nahoře na stožáru<br>
      – Zase spadla síť, Morpheus něco přepojil
    </div>
  </header>

  <div class="slideshow">
    <div class="cube">
      <div class="front"></div>
      <div class="back"></div>
      <div class="right"></div>
      <div class="left"></div>
      <div class="top"></div>
      <div class="bottom"></div>
    </div>
  </div>

  <footer>
    © 2025 SkyTel Montage – Restart systému v průběhu...
  </footer>

  <script>
    const canvas = document.getElementById("matrix");
    const ctx = canvas.getContext("2d");

    canvas.height = window.innerHeight;
    canvas.width = window.innerWidth;

    let letters = "01".split("");
    let fontSize = 14;
    let columns = canvas.width / fontSize;
    let drops = [];

    for(let x = 0; x < columns; x++) drops[x] = 1;

    function drawMatrix() {
      ctx.fillStyle = "rgba(0, 0, 0, 0.05)";
      ctx.fillRect(0, 0, canvas.width, canvas.height);

      ctx.fillStyle = "#0F0";
      ctx.font = fontSize + "px Orbitron";

      for(let i = 0; i < drops.length; i++) {
        let text = letters[Math.floor(Math.random() * letters.length)];
        ctx.fillText(text, i * fontSize, drops[i] * fontSize);

        if(drops[i] * fontSize > canvas.height && Math.random() > 0.975) drops[i] = 0;

        drops[i]++;
      }
    }

    setInterval(drawMatrix, 33);
  </script>
</body>
</html>
